{
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider"
  },
  "buildCommand": "npm run build",
  "outputDirectory": "dist/todo",
  "routes": [
    {
      "src": "^/assets/(.*)$",
      "dest": "/assets/$1"
    },
    {
      "src": "^(?!/assets/).*",
      "dest": "/index.html"
    }
  ]
}
